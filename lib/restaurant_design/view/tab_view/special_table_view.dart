import 'package:flutter/material.dart';


class SpecialTablesView extends StatelessWidget {
  const SpecialTablesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
      //  children: [columnContainer()],
      ),
    );
  }
}
